import React from "react";
import ReactDOM from "react-dom";

function Ex41() {
    const element = (
        <div>Green is the prime color of the wolrd</div>
    );
    ReactDOM.render(element, document.getElementById('root'));
};
export default Ex41;